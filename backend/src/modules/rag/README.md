# RAG module (future)

- Ingest learning materials per subject.
- Vector store (e.g. Supabase pgvector) and embeddings.
- Query pipeline: embed question → similarity search → build context → LLM with streaming.
